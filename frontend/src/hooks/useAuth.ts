import { useAuth } from '@/contexts/AuthContext';

/**
 * Convenience hook — re-exports AuthContext values.
 * Components import from here for a cleaner API surface.
 */
export { useAuth } from '@/contexts/AuthContext';
